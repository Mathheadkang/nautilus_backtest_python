# =============================================================================
# Polymarket Configuration
# =============================================================================

# Your Polygon wallet private key (without 0x prefix)
# Used for signing orders and deriving API credentials
# KEEP THIS SECRET — never commit the real .env file
POLYMARKET_PRIVATE_KEY=your_private_key_here

# Your wallet address on Polygon (the address that holds USDC)
# For EOA wallets (MetaMask, hardware wallets): this is your wallet address
# For proxy wallets (email/browser login): this is your proxy wallet address
POLYMARKET_WALLET_ADDRESS=0xYourWalletAddressHere

# Signature type for authentication
#   0 = EOA (MetaMask, Ledger, direct private key)
#   1 = Email / Magic wallet (Polymarket email login)
#   2 = Browser wallet proxy
POLYMARKET_SIGNATURE_TYPE=0

# Polymarket CLOB API host
POLYMARKET_CLOB_HOST=https://clob.polymarket.com

# Polymarket Gamma API host (market metadata)
POLYMARKET_GAMMA_HOST=https://gamma-api.polymarket.com

# Polygon chain ID (137 = mainnet, 80001 = Mumbai testnet)
POLYMARKET_CHAIN_ID=137

# =============================================================================
# Optional: Pre-derived API credentials
# If you already have API creds, set them here to skip derivation.
# Otherwise, the client will derive them from your private key.
# =============================================================================

# POLYMARKET_API_KEY=
# POLYMARKET_API_SECRET=
# POLYMARKET_API_PASSPHRASE=
